#ifndef LIB_H
#define LIB_H

// Ajouter un livre
void add_book(int id);

// Vérifier si un livre existe (1 = oui, 0 = non)
int contains_book(int id);

// Réinitialiser la librairie
void reset_lib();

#endif
